import { LoadingContainer, LoadingIcon } from "@/components/Loading/Loading";

export const Loading = () => {
  return (
    <LoadingContainer>
      <h1>Loading...</h1>
      <LoadingIcon />
    </LoadingContainer>
  );
};

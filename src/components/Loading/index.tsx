import { LoadingContainer, LoadingIcon } from "@/styles/Loading";

export const Loading = () => {
  return (
    <LoadingContainer>
      <h1>Loading...</h1>
      <LoadingIcon />
    </LoadingContainer>
  );
};
